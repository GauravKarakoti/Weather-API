# Upstash Redis Configuration
# Add these lines to your .env file to connect to your Upstash Redis instance

REDIS_HOST=winning-crab-19100.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=AUqcAAIjcDFiNjhlY2EzZTI4ODY0NjA4OTdhYzYyZTliNTJjNGY3OXAxMA
REDIS_DB=0
REDIS_TLS=true
CACHE_WARMING_ENABLED=true
CACHE_WARMING_INTERVAL_HOURS=6
BASE_URL=http://localhost:5000

# Instructions:
# 1. Copy the above configuration to your .env file
# 2. Save the .env file
# 3. Restart your server with: npm run dev
# 4. Your Weather API will now use Upstash Redis for caching!

# Security Note:
# Keep your Redis password secure and never commit it to version control
# The .env file is already in .gitignore to protect your credentials
